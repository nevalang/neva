// this is a package where we temporary keep everything that is not builtin
// we will organize it later into separate packages

component {
    #extern(int parse_int, float parse_float)
    pub ParseNum<T int | float>(data string) (res T, err error)

    #extern(scanln)
    pub Scanln(sig any) (data string)
}
