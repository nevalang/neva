// Printer and Scanner are not fundamental components
// but it's handy to be able to use them without imports.

component {
    #extern(LinePrinter)
    pub Printer<T>(data T) (sig T)

    #extern(LinePrinter)
    pub Scanner(sig any) (data string)
}