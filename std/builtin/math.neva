component {
    #extern(int int_adder, float float_adder, string string_adder)
    pub Add<T int | float | string>(seq maybe<T>) (res T)

    #extern(int int_sub, float float_sub)
    pub Sub<T int | float >(seq maybe<T>) (res T)

    #extern(int int_mul, float float_mul)
    pub Mul<T int | float >(seq maybe<T>) (res T)

    #extern(int int_decr, float float_decr)
    pub Decr<T int | float>(n T) (n T)

    #extern(int_mod)
    pub Mod(data int, [case] int) ([case] int, else int)
}
