// Not fundamental but frequently used

components {
    #runtime_func(Printer)
    pub Printer<T>(data T) (data T)
    
    #runtime_func(Parser)
    pub Parser<T int | float | bool>(s str) (res T, err error)
}