interfaces {
    pub Example(v) (v)
}

components {
    pub Read(sig) (v str)
    
    pub Print<T>(v T) (v T)
    
    pub Lock<T>(sig, v T) (v T)
    
    pub ParseNum<T int | float>(v str) (v T, err str)
    
    pub Add<T int | float | str>(a T, b T) (v T)
}