components {
    #extern(Read)
    pub Read(sig) (v str)

    #extern(Print)
    pub Print<T>(v T) (v T)

    #extern(Lock)
    pub Lock<T>(sig, v T) (v T)

    #extern(ParseInt)
    pub ParseInt(v str) (v int, err str)
}