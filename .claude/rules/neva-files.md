---
paths:
  - "**/*.neva"
---

# Neva Files

- Follow the nearest `AGENTS.md` as the scoped source of truth.
- Source of truth for style: `docs/style_guide.md`.
- Keep edited `.neva` files syntactically valid.
- If syntax is uncertain, consult `neva.g4` and a few nearby `.neva` examples before editing.
- When feasible, validate changed Neva programs by compiling or running them.
- When editing documentation or snippets, prefer a quick local compile/run check of an equivalent minimal example.
- For every AI-generated entity newly added in the current session, include at least one single-line doc comment that explains what the entity does.
