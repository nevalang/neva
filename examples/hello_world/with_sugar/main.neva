import {
	std/core
}

const {
	msg str 'Hello, World!'
}

components {
	Main(enter) (exit) {
		nodes {
			print core.Print<str>
			lock core.Lock<str>
		}
		net {
			in.enter -> lock.sig
			msg -> lock.v
			lock.v -> print.v
			print.v -> out.exit
		}
	}
}