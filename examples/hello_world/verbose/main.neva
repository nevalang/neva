import {
	std/core
}

const {
	msg str 'Hello, World!'
}

components {
	Main(enter) (exit) {
		nodes {
			#runtime_func_msg(msg)
			msg Const<str>
			print Print<str>
			lock Lock<str>
		}
		net {
			in.enter -> lock.sig
			msg.v -> lock.v
			lock.v -> print.v
			print.v -> out.exit
		}
	}
}