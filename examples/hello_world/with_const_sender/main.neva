import {
	std/core
}

const {
	msg str 'Hello, World!'
}

components {
	Main(enter) (exit) {
		nodes {
			print Print<str>
			lock Lock<str>
		}
		net {
			in.enter -> lock.sig
			$msg -> lock.v
			lock.v -> print.v
			print.v -> out.exit
		}
	}
}