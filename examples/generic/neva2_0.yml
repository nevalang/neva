types:
  user:
    name: str
    age: int
    friends: user[]

deps:
  userService:
    in:
      x: int
    out:
      y: int

in:
  usr: user
out:
  friends: user[]

workers:
  usrSvc: userService

net:
  in:
    usr:
      usrSvc: [x]
