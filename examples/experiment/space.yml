# space.yml

# space is what compiler turns into one big runtime program

meta: ... # want compiler version
require: ... # other spaces dependencies
main: ... # relative path to main package

# pkg

interfaces:
  interfaceName:
    in:
      sig: any
    out:
      sig: any

types:
  user:
    params: [T]
    body:
      name:
        type: int
      data:
        type: T
      friends: map