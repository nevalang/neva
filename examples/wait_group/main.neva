import { sync }

flow Main(start) (stop) {
  wg sync.WaitGroup
  Println
  ---
  :start -> [
    ('Hello' -> println -> wg:sig),
    ('Neva' -> println -> wg:sig),
    ('World!' -> println -> wg:sig)
  ]
  3 -> wg:count
  wg -> :stop
}