meta:
  compiler: 0.0.1

import:
  local: [root]
  std: [math, logic]
  global:
    shared: 0.0.1

scope:
  "*": "std.math.mul"
  "%": "std.math.remainder"
  ">": "std.logic.more"
  "f": "std.logic.filter"
  "bp": "global.shared.bypass"
  "root": "local.root"

export:
  - root

root: root
