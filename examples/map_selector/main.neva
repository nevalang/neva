const {
    k str 'alice'
    m map<int> { alice: 32 }
}

components {
    Main(enter) (exit) {
        nodes {
            print Print<int>
            selector MapSelector<int>
        }

        net {
            in.enter -> (
                $m -> selector.v
                $k -> selector.k
            )
            selector.ok -> print.v
            print.v -> out.exit
        }
    }
}