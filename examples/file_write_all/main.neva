import { io, fmt }

def Main(start any) (stop any) {
	io.WriteAll, Panic
	---
	:start -> [
		'file_writer_example.txt' -> writeAll:filename,
		'Hello, io.WriteAll!' -> writeAll:data
	]
	writeAll:err -> panic
	writeAll:sig -> :stop
}
