import {
    fmt
    runtime
}

def Main(start any) (stop any) {
	scanln fmt.Scanln
	println fmt.Println
	panic runtime.Panic
	---
	:start -> scanln -> println:data
	println:res -> :stop
	[println:err, scanln:err] -> panic
}
