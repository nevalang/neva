// This program reads a str and prints it.
// And it does so in an infinite loop.

import {
	std/core
}

components {
	Main(enter) (exit) {
		nodes {
			read core.Read
			print Print<str>
		}
		net {
			in.enter -> read.sig
			read.v -> print.v
			print.v -> read.sig
		}
	}
}