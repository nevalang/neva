import {
    fmt
    runtime
    lists
}

def Main(start any) (stop any) {
    stream_to_list lists.FromStream<int>
    println fmt.Println<list<int>>
    panic runtime.Panic
    ---
    :start -> 1..11 -> stream_to_list -> println:data
    println:res -> :stop
    println:err -> panic
}
