component Main(start) (stop) {
  nodes {
    r1 Range,
    r2 Range,
    Zip<int, int>,
    For<ZipRes<int, int>>{Println<ZipRes<int, int>>}
  }
  :start -> [
    (0 -> [r1:from, r2:from]),
    (3 -> [r1:to, r2:to])
  ]
  r1 -> zip:first
  r2 -> zip:second
  zip -> for -> :stop
}