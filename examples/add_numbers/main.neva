import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    add Add
    println fmt.Println<any>
    panic runtime.Panic
    ---
    :start -> [
        1 -> add:left,
        2 -> add:right
    ]
    add -> println
    println:res -> :stop
    println:err -> panic
}
