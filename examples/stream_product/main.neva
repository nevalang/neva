import { streams, fmt }

def Main(start any) (stop any) {
	FanOut
	streams.Product<int, int>
	For<int>{fmt.Println<int>}
	Wait
	---
	:start -> [
		0..3 -> product:first,
		0..3 -> product:second
	]
	product -> for -> wait -> :stop
}
