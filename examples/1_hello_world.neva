use {
	std
}

const {
	msg string = 'Hello, World!'
}

components {
	Main(start) (exit) {
		nodes {
			lock = std.Lock()
			print = std.Print()
		}
		net {
			in.start -> lock.sig
			const.msg -> lock.v
			lock.v -> print.v[0]
			print.v -> out.exit
		}
	}
}