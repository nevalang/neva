use {
	std
}

const {
	msg string = 'Hello, World!'
}

components {
	Main(enter) (exit) {
		nodes {
			lock = std.Lock()
			print = std.Print()
		}
		net {
			in.enter -> lock.sig
			const.msg -> lock.v
			lock.v -> print.v
			print.v -> out.exit
		}
	}
}