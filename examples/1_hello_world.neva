use {
	std std
}

const {
	msg string = "Hello, World!"
}

components {
	Main(enter) (exit) {
		nodes {
			lock = std.Lock()
			print = std.Print()
		}
		net {
			in.enter -> lock.in.sig
			const.msg -> lock.out.v
			lock.in.v -> print.out.v
			print.in.v -> out.exit
		}
	}
}