component Main(start any) (stop any) {
	nodes {
		Println<int>
		mul PortReducer<int> { handler Multiplier<int> }
		// mul PortReducer<int> { Multiplier<int> }
	}
	net {
		:start -> (
			1 -> mul:ports[0],
			2 -> mul:ports[1],
			3 -> mul:ports[2]
		)
		mul:res -> println:data
		println:sig -> :stop
	}
}
