use {
	std/tmp
}

const {
	msg str 'Hello, World!'
}

components {
	Main(enter) (exit) {
		nodes {
			lock tmp.Lock<str>
			print tmp.Print<str>
		}
		net {
			in.enter -> lock.sig
			msg -> lock.v
			lock.v -> print.v
			print.v -> out.exit
		}
	}
}