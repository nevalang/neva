component Main(start) (stop) {
	nodes { Println<any>, Split, Join }
	net {
		:start -> (
			'neva' -> split:data,
			'' -> split:delim
		)
		split:res -> join:data
		join:res -> println:data
	    println:sig -> :stop
	}
}

