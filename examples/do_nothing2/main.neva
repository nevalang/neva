// Program that does literally nothing.

import {
	std/core
}

components {
	Main(enter) (exit) {
		nodes {
			lock core.Lock<any>
		}
		net {
			in.enter -> {
				lock.sig
				lock.v
			}
			lock.v -> out.exit
		}
	}
}