import { lists, fmt }

const lst list<int> = [50, 30, 20, 100]

def Main(start any) (stop any) {
    s2l StreamToList
    map Map<int, int>{Dec<int>}
    println fmt.Println
    l2s ListToStream
    ---
    :start -> $lst -> l2s -> map -> s2l -> println -> :stop
}
