import { lists, fmt }

const lst list<int> = [50, 30, 20, 100]

def Main(start) (stop) {
    ListToStream<int>
    Map<int, int>{Dec<int>}
    List<int>
    fmt.Println
    ---
    :start -> { $lst -> listToStream -> map -> list -> println -> :stop }
}
