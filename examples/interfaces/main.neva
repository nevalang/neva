import { fmt }

def Main(start any) (stop any) {
    wrapper Wrapper{fmt.Println}
    ---
    :start -> wrapper -> :stop
}

def Wrapper(msg any) (msg any) {
    printer IPrinter<any>
    ---
    :msg -> printer -> :msg
}

interface IPrinter<T>(T) (T)
