import {
    fmt
    lists
    runtime
}

const f int = 69
const l list<int> = [320, 420, 100, -100, 0, 5]

def Main(start any) (stop any) {
    push lists.Push<int>
    println fmt.Println<list<int>>
    panic runtime.Panic
	---
	:start -> [
        $l -> push:lst,
        $f -> push:data
	]
    push -> println:data
    println:res -> :stop
    println:err -> panic
}
