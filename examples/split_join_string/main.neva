import { strings, fmt }

def Main(start) (stop) {
	fmt.Println, strings.Split, strings.Join
	---
	:start -> [
		{ 'neva' -> split:data },
		{ '' -> split:delim }
	]
	split -> join -> println -> :stop
}

