import { strings, fmt }

def Main(start any) (stop any) {
	fmt.Println, strings.Split, strings.Join, Panic
	---
	:start -> [
		'neva' -> split:data,
		'' -> split:delim
	]
	split -> join -> println:data
	println:res -> :stop
	println:err -> panic
}

