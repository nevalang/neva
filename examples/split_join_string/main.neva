import { strings, fmt }

def Main(start any) (stop any) {
	fmt.Println, strings.Split, strings.Join
	---
	:start -> [
		'neva' -> split:data,
		'' -> split:delim
	]
	split -> join -> println -> :stop
}

