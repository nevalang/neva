linters:
  enable-all: true
  disable:
    - godox
    - godot
    - gomnd
    - forbidigo
    - nlreturn
    - wsl

linters-settings:
  lll:
    line-length: 120

issues:
  exclude-rules:
    - linters:
        - lll
      source: "^//go:generate "
