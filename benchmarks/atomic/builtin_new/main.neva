// Benchmarks const sender path in one-shot mode.
// Compiler lowers const sender to virtual builtin New wiring.
const value int = 42

// Main emits a const int sender, exercising compiler-lowered virtual New behavior.
def Main(start any) (stop any) {
	:start -> $value -> :stop
}
