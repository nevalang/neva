import { sync }

// Main sends one start signal into both fan_in inports and waits for both arrivals.
def Main(start any) (stop any) {
	fan_in FanIn<any>
	wg sync.WaitGroup
	---
	:start -> [
		fan_in:data[0],
		fan_in:data[1],
		2 -> wg:count
	]
	fan_in -> true -> wg:sig
	wg -> :stop
}
