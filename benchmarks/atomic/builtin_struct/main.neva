// Benchmarks builtin component `Struct` in one-shot mode.
type User struct {
	name string
	age int
}

def Main(start any) (stop any) {
	builder Struct<User>
	---
	:start -> [
		'John' -> builder:name,
		32 -> builder:age
	]
	builder -> :stop
}
