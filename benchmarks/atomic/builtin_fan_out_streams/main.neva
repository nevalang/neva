import { sync }

def Main(start any) (stop any) {
	fan_out FanOut<any>
	done FanIn<any>
	wg sync.WaitGroup
	---
	:start -> [
		fan_out,
		2 -> wg:count
	]
	fan_out:data[0] -> true -> done:data[0]
	fan_out:data[1] -> true -> done:data[1]
	done -> wg:sig
	wg -> :stop
}
