// Benchmarks builtin component `Ternary` else-path in one-shot mode.
def Main(start any) (stop any) {
	ternary Ternary<int>
	---
	:start -> [
		false -> ternary:if,
		42 -> ternary:then,
		0 -> ternary:else
	]
	ternary -> :stop
}
