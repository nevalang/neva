type Event<T> union {
	Data T
}

def Main(start any) (stop any) {
	switch Switch<Event<int>>
	---
	:start -> [
		Event::Data -> switch:case[0],
		Event<int>::Data(42) -> switch:data
	]
	switch:case[0] -> :stop
}
