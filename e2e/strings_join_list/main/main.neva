import { strings, fmt, runtime }

def Main(start any) (stop any) {
        fmt.Println, strings.Join, strings.Split, runtime.Panic
        ---
        :start -> [
                'hello neva lang' -> split:data,
                ' ' -> split:delim,
                ' ' -> join:sep
        ]
        split -> join:data
        join -> println:data
        println:res -> :stop
        println:err -> panic
}
