import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    and BitAnd
    println fmt.Println
    panic runtime.Panic
    ---
    :start -> [
        5 -> and:left,
        3 -> and:right
    ]
    and:res -> println
    println:res -> :stop
    println:err -> panic
}
