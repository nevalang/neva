import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    rshift BitRsh
    println fmt.Println
    panic runtime.Panic
    ---
    :start -> [
        8 -> rshift:left,
        1 -> rshift:right
    ]
    rshift:res -> println
    println:res -> :stop
    println:err -> panic
}