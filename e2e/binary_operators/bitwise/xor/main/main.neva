import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    xor BitXor
    println fmt.Println
    panic runtime.Panic
    ---
    :start -> [
        5 -> xor:left,
        3 -> xor:right
    ]
    xor:res -> println
    println:res -> :stop
    println:err -> panic
}
