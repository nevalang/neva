import { fmt, runtime }

def Main(start any) (stop any) {
    fmt.Println
    panic runtime.Panic
    ---
    :start -> { (15.0 / 3.0) -> println:data }
    println:res -> :stop
    println:err -> panic
}
