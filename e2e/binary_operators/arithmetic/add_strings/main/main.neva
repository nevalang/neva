import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    add Add
    println fmt.Println
    panic runtime.Panic
    ---
    :start -> [
        'Hello, ' -> add:left,
        'World!' -> add:right
    ]
    add:res -> println
    println:res -> :stop
    println:err -> panic
}
