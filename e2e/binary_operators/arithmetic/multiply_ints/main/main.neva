import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    mul Mul
    println fmt.Println
    panic runtime.Panic
    ---
    :start -> [
        5 -> mul:left,
        3 -> mul:right
    ]
    mul:res -> println
    println:res -> :stop
    println:err -> panic
}
