import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    pow Pow
    println fmt.Println<any>
    panic runtime.Panic
    ---
    :start -> [
        2 -> pow:left,
        3 -> pow:right
    ]
    pow:res -> println
    println:res -> :stop
    println:err -> panic
}
