import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    fmt.Println
    panic runtime.Panic
    ---
    :start -> { (2 ** 3) -> println:data }
    println:res -> :stop
    println:err -> panic
}
