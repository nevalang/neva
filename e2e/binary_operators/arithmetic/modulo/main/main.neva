import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    println fmt.Println
    panic runtime.Panic
    ---
    :start -> { (7 % 3) -> println:data }
    println:res -> :stop
    println:err -> panic
}