import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    fmt.Println
    panic runtime.Panic
    ---
    :start -> { (true || false) -> println:data }
    println:res -> :stop
    println:err -> panic
}