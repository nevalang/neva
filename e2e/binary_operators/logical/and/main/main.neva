import {
    fmt
    runtime
}

def Main(start any) (stop any) {
    fmt.Println
    panic runtime.Panic
    ---
    :start -> { (true && true) -> println:data }
    println:res -> :stop
    println:err -> panic
} 