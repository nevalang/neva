type MaybeInt union {
	Int int
}

type Other union {
	Str string
}

def Main(start any) (stop any) {
	union_node Union<MaybeInt>
	---
	:start -> [
		Other::Str('x') -> union_node:tag,
		1 -> union_node:data
	]
	union_node -> :stop
}
