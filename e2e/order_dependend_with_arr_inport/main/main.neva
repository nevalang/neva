component Main(start) (stop) {
	nodes {
		Println<int>
		sub PortReducer<int> { handler Subtractor<int> }
	}
	net {
		:start -> (
			1 -> sub:ports[0],
			2 -> sub:ports[1],
			3 -> sub:ports[2]
		)
		sub:res -> println:data
		println:sig -> :stop
	}
}
