import {
    fmt
    runtime
}

const l list<int> = [1, 2, 3]

def Main(start any) (stop any) {
	fmt.Println<int>
	Reduce<int, int>{Sub}
	ListToStream<int>
	panic runtime.Panic
	---
	:start -> $l -> listToStream -> reduce:data
	0 -> reduce:init
	reduce -> println:data
	println:res -> :stop
	println:err -> panic
}
