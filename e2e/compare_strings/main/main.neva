import { fmt }

def Main(start any) (stop any) {
    fmt.Println, Gt<string>, Ternary
    ---
    :start -> [
        'Z' -> gt:left,
        'A' -> gt:right
    ]
    gt:res -> ternary:if
    'Actual is greater' -> ternary:then
    'Actual is lower' -> ternary:else
    ternary -> println -> :stop
}
