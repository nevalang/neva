import {
    builtin
    fmt
    os
}

def Main(start any) (stop any) {
    exit os.Exit
    lock builtin.Lock<int>
    scan fmt.Scanln
    del builtin.Del
    ---
    :start -> [2 -> exit:code, scan, 2 -> lock:data]
    scan:res -> lock:sig
    scan:err -> del
    lock:res -> :stop
}
