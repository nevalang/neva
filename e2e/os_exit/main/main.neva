import {
	fmt
	os
}

def Main(start any) (stop any) {
	exit os.Exit
	lock Lock<int>
	scanln fmt.Scanln
	del Del
	---
	:start -> [
		2 -> exit:code,
		scanln,
		2 -> lock:data,
	]
	scanln:res -> lock:sig
	scanln:err -> del
	lock -> :stop
}
