{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "runtime",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/runtime",
      "cwd": "${workspaceFolder}"
    }
  ]
}
