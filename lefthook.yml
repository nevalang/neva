pre-commit:
  commands:
    gofix-check:
      run: make gofix-check
    gofmt:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true

pre-push:
  commands:
    lint:
      run: make lint
    test-unit:
      run: make test-unit
    vulncheck:
      run: make vulncheck
