// Package messages defines immutable Neva values and pure operations over them.
//
// It intentionally depends only on the Go standard library. Ports, ordering,
// tracing, program execution, and runtime functions belong to parent runtime
// packages.
package messages
