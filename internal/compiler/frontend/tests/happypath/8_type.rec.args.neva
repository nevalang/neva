type {
    Rec { foo vec<int> }
}

type {
    Rec {
        foo vec<int> }
}

type {
    Rec { foo vec<int>
    }
}

type {
    Rec {
        foo vec<int>
    }
}

type {
    Rec {
        foo vec<int>
        bar map<str, int>
    }
}

type {
    Rec { foo vec<int>
        bar map<str, int>
    }
}

type {
    Rec {
        foo vec<int>
        bar map<str, int> }
}

type {
    Rec {
        foo vec<int>

        bar map<str, int>
    }
}

type {
    Rec {

        foo vec<int>
        bar map<str, int>
    }
}

type {
    Rec {
        foo vec<int>
        bar map<str, int>

    }
}

type {
    Rec {

        foo vec<int>
        bar map<str, int>

    }
}

type {
    Rec {

        foo vec<int>

        bar map<str, int>

    }
}

type {
    Rec {
        foo map<str, int>
        bar vec<{ Monday, Tuesday, Wednesday, Thursday, Friday }>
    }
}

type {
    Rec {
        foo map<str, int>
        bar vec<{
            Monday, Tuesday, Wednesday, Thursday, Friday }>
    }
}

type {
    Rec {
        foo map<str, int>
        bar vec<{ Monday, Tuesday, Wednesday, Thursday, Friday
        }>
    }
}

type {
    Rec {
        foo map<str, int>
        bar vec<{
            Monday, Tuesday, Wednesday, Thursday, Friday
        }>
    }
}

type {
    Rec {
        foo map<str, int>
        bar vec<{
            Monday,
            Tuesday,
            Wednesday,
            Thursday,
            Friday
        }>
    }
}