io { IReader1 (arr [512]int) () }

io {
    IReader1 (arr [512]int) ()
}

io { IReader1 (arr [512]int) (arr [1024]bool) }

io {
    IReader1 (arr [512]int) (arr [1024]bool)
}

io { IReader1 (arr [512]{ Monday, Tuesday }) (arr [1024]{ January, February }) }

io {
    IReader1 (arr [512]{ Monday, Tuesday }) (arr [1024]{ January, February })
}

io { IReader1 (arr [512]{ foo int }) (arr [1024]{ bar bool }) }

io {
    IReader1 (arr [512]{ Monday, Tuesday }) (arr [1024]{ January, February })
}

io { IReader1 (arr [512]{ Monday, Tuesday }, arr2 [256]{ foo int }) (arr [1024]{ January, February }) }

io {
    IReader1 (arr [512]{ Monday, Tuesday }, arr2 [256]{ foo int }) (arr [1024]{ January, February })
}

io {
    IReader1 (
        arr [512]{ Monday, Tuesday },
        arr2 [256]{ foo int }
    ) (arr [1024]{ January, February })
}

io {
    IReader1 (
        arr [512]{ Monday, Tuesday },
        arr2 [256]{ foo int }
    ) (arr [1024]{ January, February }, arr2 [256]{ bar bool })
}

io {
    IReader1 (
        arr [512]{ Monday, Tuesday },
        arr2 [256]{ foo int }
    ) (
        arr [1024]{ January, February },
        arr2 [256]{ bar bool }
    )
}

io {
    IReader1 (
        arr [512]{ Monday, Tuesday },
        arr2 [256]{ foo int },
        arr3 [128]{ baz {} }
    ) (
        arr [1024]{ January, February },
        arr2 [256]{ bar bool },
        arr3 [128][64]{ bax {} }
    )
}

