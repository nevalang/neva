io {}

io {
}

io {

}

io {
    IReader1() ()
}

io {
    IReader2() ()
}

io {
    IReader3( 
    ) (
    )
}

io {
    IReader4(
        x int) ()
}

io {
    IReader5(x int
    ) ()
}

io {
    IReader6(
        x int
    ) ()
}

io {
    IReader7(x int) (z str)
}

io {
    IReader8(x int) (
        z str)
}

io {
    IReader9(x int) (z str
    )
}

io {
    IReader10(
        x int) (
            z str)
}

io {
    IReader11(
        x int
    ) (z str)
}

io {
    IReader12(
        x int
    ) (
        z str
    )
}

io {
    IReader13(x int, y bool) (z str)
}

io {
    IReader14(
        x int, y bool) (z str)
}

io {
    IReader15(x int,
    y bool) (z str)
}

io {
    IReader16(x int, y bool) (
        z str)
}

io {
    IReader17(
        x int,
        y bool) (z str)
}

io {
    IReader18(
        x int,
        y bool) (
            z str)
}

io {
    IReader19(
        x int,
        y bool
    ) (z str)
}

io {
    IReader20(
        x int,
        y bool
    ) (
        z str)
}

io {
    IReader21(
        x int,
        y bool
    ) (
        z str
    )
}

io {
    IReader22(x int, y bool) (z1 str, z2 float)
}

io {
    IReader23(x int, y bool) (
        z1 str, z2 float)
}

io {
    IReader24(x int, y bool) (z1 str,
    z2 float)
}

io {
    IReader25(x int, y bool) (z1 str, z2 float
    )
}

io {
    IReader26(x int, y bool) (
        z1 str, z2 float
    )
}

io {
    IReader27(x int, y bool) (
        z1 str,
        z2 float
    )
}

io {
    IReader28() ()
    IReader29() (z int)
    IReader30() (z1 int, z2 str)
    IReader31(x int) ()
    IReader32(x int, y str) ()
    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)
}

io {
    IReader28() ()

    IReader29() (z int)
    IReader30() (z1 int, z2 str)
    IReader31(x int) ()
    IReader32(x int, y str) ()
    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)
}

io {
    IReader28() ()
    IReader29() (z int)

    IReader30() (z1 int, z2 str)
    IReader31(x int) ()
    IReader32(x int, y str) ()
    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)
}

io {
    IReader28() ()
    IReader29() (z int)

    IReader30() (z1 int, z2 str)

    IReader31(x int) ()
    IReader32(x int, y str) ()
    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)
}

io {

    IReader28() ()
    IReader29() (z int)

    IReader30() (z1 int, z2 str)
    IReader31(x int) ()
    IReader32(x int, y str) ()

    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)
}

io {
    IReader28() ()
    IReader29() (z int)

    IReader30() (z1 int, z2 str)
    IReader31(x int) ()
    IReader32(x int, y str) ()

    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)

}

io {

    IReader28() ()
    IReader29() (z int)

    IReader30() (z1 int, z2 str)
    IReader31(x int) ()
    IReader32(x int, y str) ()

    IReader33(x int) (z bool)
    IReader34(x int, y str) (z1, z2 bool)

}

io {

    IReader28() (

    )
    IReader29(

    ) (z int)

    IReader30() (z1 int,
    z2 str)
    IReader31(
        x int) ()
    IReader32(x int,
    y str) ()

    IReader33(x int) (z bool
    )
    IReader34(x int, y str) (
        z1, z2 bool
    )

}