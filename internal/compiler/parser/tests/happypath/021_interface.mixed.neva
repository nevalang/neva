interfaces {
    IReader1 () ()
    IReader20 (x int, y string) (a int, b bool)

    IReader12 (
        x enum {
            Monday,
            Tuesday,
            Wednesday
        }, y enum {
            January,
            February
        }
    ) (
        a enum {
            Monday,
            Tuesday,
            Wednesday
        }, b  enum {
            January, February
        }
    )

    IReader1 (
        arr [512]enum { Monday, Tuesday },
        arr2 [256]struct { foo int },
        arr3 [128]struct { baz struct {} }
    ) (
        arr [1024]enum { January, February },
        arr2 [256]struct { bar bool },
        arr3 [128][64]struct { bax struct {} }
    )

    IReader1 (x struct {
        foo int
        bar string
    }) (a struct {
        baz bool
        bax float })

    IReader1 (
        x struct { foo struct { a int } },
        y struct {}
    ) (
        bar struct { baz struct { b bool } },
        bax struct { r struct {} }
    )

    pub IReader1 (
        a bool | int | struct { foo int | string },
        b [256][128]struct { bar int | [64]int }
    ) (
        x string |
            float |
            enum { Monday } |
            struct {
                foo enum { January } | struct {}
            }
    )
}