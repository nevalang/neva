components {}

components {
}

components {

}

components { Doer() () {} }

components {
    Doer() () {} }

components { Doer() () {}
}

components {
    Doer() () {}
}

components { Doer<T>(x int) (y T) {} }

components {
    Doer<T>(x int) (y T) {
    }
}

components {
    Doer<T>(x int) (y T) {

    }
}

components {
    Doer<T>(x int) (y T) {
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {}
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes { absNode INode }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            absNode INode }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes { absNode INode
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            absNode INode
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            simpleAbsNode INode
            absNodeWithTypeArgs INode<
                int,
                map<
                    str, vec<bool>
                >
            >
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes { concreteNode  Node() }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            concreteNode  Node() }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes { concreteNode  Node()
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            concreteNode  Node()
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            concreteNodeWithTypeArgs  Node<int, vec<str>>()
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            concreteNodeWithDI1  Node1(foo: Node2())
            concreteNodeWithDI2  Node1(foo: Node2(), bar: Node3())
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            concreteNodeWithDI1  Node1(
                foo: Node2()
            )
            concreteNodeWithDI2  Node1(
                foo: Node2(),
                bar: Node3()
            )
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            concreteNodeWithDIAndTypeArgs  Node1<
                int,
                map<
                    str,
                    vec<bool>
                >
            >(
                foo: Node2(),
                bar: Node3()
            )
        }
    }
}

components {
    Doer<T>(x int) (y T) {
        nodes {
            simpleAbsNode INode

            absNodeWithTypeArgs INode<
                int,
                map<
                    str, vec<bool>
                >
            >

            concreteNodeWithDI1  Node1(foo: Node2())

            concreteNodeWithDIAndTypeArgs  Node1<
                int,
                map<
                    str,
                    vec<bool>
                >
            >(
                foo: Node2(),
                bar: Node3()
            )
        }
    }
}

components {
	Main(enter) (exit) {
		nodes {
			abs IReader
			concrete  io.Reader()
		}
		net {
			in.enter -> abs.v
			abs.v -> {
				concrete.v
			}
			concrete.v -> out.code
		}
	}
}