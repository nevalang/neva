interfaces { IReader1 (x struct { foo struct {} }) () }

interfaces {
    IReader1 (x struct { foo struct {} }) () }

interfaces {
    IReader1 (x struct { foo struct {} }) ()
}

interfaces { IReader1 (x struct { foo struct {} }) (bar struct { baz struct {} }) }

interfaces {
    IReader1 (x struct { foo struct {} }) (bar struct { baz struct {} })
}

interfaces {
    IReader1 (
        x struct { foo struct {} }
    ) (bar struct { baz struct {} })
}

interfaces {
    IReader1 (
        x struct { foo struct {} }
    ) (
        bar struct { baz struct {} }
    )
}

interfaces {
    IReader1 (x struct {
        foo struct { a int }
    }) (bar struct {
        baz struct { b bool }
    })
}

interfaces {
    IReader1 (
        x struct { foo struct { a int } }
    ) (
        bar struct { baz struct { b bool } }
    )
}

interfaces {
    pub IReader1 (
        x struct { foo struct { a int } },
        y struct {}
    ) (
        bar struct { baz struct { b bool } },
        bax struct { r struct {} }
    )
}