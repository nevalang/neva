interface {}

interface {
}

interface {

}

interface { IReader1 () () }

interface { IReader2 () ()
}

interface {
    IReader3 () () }

interface {
    IReader4 () ()
}

interface {
    IReader5 (
    ) ()
}

interface {
    IReader6 () (
    )
}

interface {
    IReader7 (
    ) (
    )
}

interface { IReader8 (x int) () }

interface { IReader8 (
    x int) () }

interface { IReader8 (x int
) () }

interface { IReader8 (x int) (
) }

interface {
    IReader8 (x int) () }

interface { IReader8 (x int) ()
}

interface {
    IReader8 (x int) ()
}

interface {
    IReader8 (
        x int) ()
}

interface {
    IReader8 (x int
    ) ()
}

interface {
    IReader8 (x int) (
    )
}

interface {
    IReader8 (
        x int
    ) ()
}

interface {
    IReader8 (
        x int
    ) (
    )
}

interface { IReader8 (x int, y string) () }

interface {
    IReader8 (x int, y string) () }

interface { IReader8 (x int, y string) ()
}

interface { IReader8 (
    x int, y string) () }

interface { IReader8 (x int,
y string) () }

interface {
    IReader8 (x int, y string) ()
}

interface {
    IReader8 (
        x int, y string
    ) ()
}

interface {
    IReader8 (
        x int,
        y string) ()
}

interface {
    IReader8 (
        x int,
        y string
    ) ()
}

interface {
    IReader8 (
        x int,
        y string
    ) (
    )
}

interface {
    IReader8 (
        x int,
        y string
    ) ()
}

interface { IReader9 () (a int) }

interface {
    IReader10 () (a int) }

interface { IReader11 () (a int)
}

interface {
    IReader12 () (a int)
}

interface {
    IReader13 (
    ) (a int)
}

interface {
    IReader14 () (
        a int)
}

interface {
    IReader15 () (a int
    )
}

interface {
    IReader16 (
    ) (
        a int
    )
}

interface { IReader17 (x int) (a int) }

interface {
    IReader18 (x int) (a int)
}

interface {
    IReader19 (x int, y string) (a int)
}

interface {
    pub IReader20 (x int, y string) (a int, b bool)
}

interface {
    pub IReader21 (x, y) (a, b)
}