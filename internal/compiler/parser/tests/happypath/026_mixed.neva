use {
	std io
}

types {
	MyInt int
	MyFloat float
	MyStr str
	MyBool bool
}

interfaces {
	IReader(path string) (i int, e err)
	IWriter(path) (i int, anything)
}

components {
	Main(enter) (exit) {
		nodes {
			abs IReader
			concrete  io.Reader()
		}
		net {
			in.enter -> abs.v
			abs.v -> {
				concrete.v
			}
			concrete.v -> out.code
		}
	}
}