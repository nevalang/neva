types {
    EmptyRec struct {}
}

types {
    EmptyRec2 struct {
    }
}

types {
    EmptyRec2 struct {

    }
}

types {
    Rec struct  { foo int }
}

types {
    Rec struct {
        foo int }
}

types {
    Rec struct { foo int
    }
}

types {
    Rec struct {
        foo int
    }
}

types {
    Rec struct {
        foo int
        bar enum { Monday, Tuesday, Wednesday }
    }
}

types {
    Rec struct { foo int
        bar enum { Monday, Tuesday, Wednesday }
    }
}

types {
    Rec struct {
        foo int
        bar enum { Monday, Tuesday, Wednesday } }
}

types {
    Rec struct {
        foo int
        bar enum {
            Monday,
            Tuesday,
            Wednesday
        }
    }
}

types {
    Rec struct {
        foo int

        bar enum {
            Monday,
            Tuesday,
            Wednesday
        }
    }
}

types {
    EmptyRec struct {}

    Rec struct { foo int }

    pub Rec struct {
        foo int
        bar enum {
            Monday,
            Tuesday,
            Wednesday
        }
    }
}