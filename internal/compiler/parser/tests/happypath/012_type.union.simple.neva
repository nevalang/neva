types { num int | float }

types {
    num int | float
}

types { num int | float
}

types {
    num int | float }

types {
    num int
        | float
}

types { num int
        | float
}

types {
    num int
        | float }

types {
    num int |
        float
}

types { num int |
        float
}

types {
    num int |
        float }

types {
    time enum { Monday, Tuesday, Wednesday } | enum { January, February, March }
}


types { time enum { Monday, Tuesday, Wednesday } | enum { January, February, March }
}


types {
    time enum { Monday, Tuesday, Wednesday } | enum { January, February, March } }

types {
    time enum {
        Monday, Tuesday, Wednesday } | enum { January, February, March }
}

types {
    time enum { Monday, Tuesday, Wednesday } | enum { January, February, March
    }
}

types {
    time enum {
        Monday, Tuesday, Wednesday } | enum { January, February, March
    }
}

types {
    time enum {
        Monday, Tuesday, Wednesday
    } | enum { January, February, March }
}

types {
    time enum {
        Monday, Tuesday, Wednesday
    } | enum {
        January, February, March }
}

types {
    time enum {
        Monday, Tuesday, Wednesday
    } | enum {
        January, February, March
    }
}

types {
    time enum {
        Monday,
        Tuesday,
        Wednesday
    } | enum {
        January,
        February,
        March
    }
}

types { Obj struct {} | struct { foo int } }

types {
    Obj struct {} | struct { foo int } }

types { Obj struct {} | struct { foo int }
}

types {
    Obj struct {} | struct { foo int }
}

types {
    Obj struct {} | struct { foo int | float }
}

types {
    SomeTyp struct { foo int | float } | int | enum {
        Monday,
        Tuesday,
        Wednesday
    } | enum {
        January,
        February,
        March
    } | float 
}

types { size [64]int | [32]int }

types {
    size [64]int | [32]int
}

types {
    size [64]int
        | [32]int
}

types {
    size [64]int |
        [32]int
}

types {
    T int | enum { Monday, Tuesday, Wednesday } | [64]int | [32]int | struct { foo int | float }
}

types {
    pub T int | enum {
        Monday, Tuesday, Wednesday
    } | [64]int | [32]int | struct {
        foo int | float
    }
}

