interfaces { IReader1 (a bool | int) () }

interfaces {
    IReader1 (a bool | int) ()
}

interfaces {
    IReader1 () (x string | float)
}

interfaces {
    IReader1 (a bool | int) (x string | float)
}

interfaces {
    IReader1 (a bool | int | struct {}) (x string | float | enum { Monday })
}

interfaces {
    IReader1 (
        a bool | int | struct { foo int | string }
    ) (
        x string |
        float |
        enum { Monday } |
        struct {
            foo enum { January } | struct {}
        }
    )
}

interfaces {
    pub IReader1 (
        a bool | int | struct { foo int | string },
        b [256][128]struct { bar int | [64]int }
    ) (
        x string |
            float |
            enum { Monday } |
            struct {
                foo enum { January } | struct {}
            }
    )
}
