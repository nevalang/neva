interfaces { IReader1 (a bool | int) () }

interfaces {
    IReader1 (a bool | int) ()
}

interfaces {
    IReader1 () (x str | float)
}

interfaces {
    IReader1 (a bool | int) (x str | float)
}

interfaces {
    IReader1 (a bool | int | struct {}) (x str | float | enum { Monday })
}

interfaces {
    IReader1 (
        a bool | int | struct { foo int | str }
    ) (
        x str |
        float |
        enum { Monday } |
        struct {
            foo enum { January } | struct {}
        }
    )
}

interfaces {
    pub IReader1 (
        a bool | int | struct { foo int | str },
        b [256][128]struct { bar int | [64]int }
    ) (
        x str |
            float |
            enum { Monday } |
            struct {
                foo enum { January } | struct {}
            }
    )
}
