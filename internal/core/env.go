package core

// type Compiler struct {}

// func (c Compiler) Compile(Program) {

// }

// type Program struct {
// 	mods map[string]Module
// 	main    string
// }

// func NewProgram(mods map[string]Module, main string) (Program, error) {
// 	if mods[]

// 	return Program{mods, main}, nil
// }

// func (r Runtime) resolveDeps(deps Interfaces) error {
// 	for dep := range deps {
// 		mod, ok := r.env[dep]
// 		if !ok {
// 			return errModNotFound(dep)
// 		}

// 		i := mod.Interface()
// 		err := i.Compare(deps[dep])
// 		if err != nil {
// 			return fmt.Errorf("unresolved dependency '%s': %w", dep, err)
// 		}
// 	}

// 	return nil
// }
